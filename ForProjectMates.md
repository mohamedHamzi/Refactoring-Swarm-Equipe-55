## after cloning do the following:
- install python 3.11.9 installer and install it.
- open the project folder and open a terminal and run the following commands:
    - py -3.11 -m venv venv
    - venv\Scripts\activate
    - pip install -r requirements.txt
    - python check_setup.py
- if all is done, you can start the project.

